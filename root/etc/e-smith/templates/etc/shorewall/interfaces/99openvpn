#
# 99openvpn
#
{
    $mode = $openvpn{'Mode'} || 'routed';
    if ($mode eq 'routed') {
        $OUT .= "ovpn        tun+\n";
    } elsif ($mode eq 'bridged') {
        my $tap = $openvpn{'TapInterface'} || 'tap0';
        $OUT .= "dev $tap\n";
    }
}
